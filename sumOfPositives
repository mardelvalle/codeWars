function positiveSum(arr) {
  let total = 0
  if (arr===[]){
    return 0
  }
  else {
  for (let i=0; i < arr.length; i++){
    if (arr[i]>0){
      total=total+arr[i]
    }  
  }
  return(total)
}
}
